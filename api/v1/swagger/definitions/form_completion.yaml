---
type: object
properties:
  form_id:
    description: "the form identity"
    type: string
  form_fields:
    description: "list of field completions"
    type: array
    items:
      $ref: form_field_completion.yaml
additionalProperties: false
required:
  - form_id

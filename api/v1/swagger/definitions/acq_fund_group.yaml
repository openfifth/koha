---
type: object
properties:
  fund_group_id:
    type:
      - integer
      - "null"
    readOnly: true
    description: Internal fund group identifier
  name:
    type:
      - string
      - "null"
    description: Fund group name
  description:
    type:
      - string
      - "null"
    description: Description
  currency:
    type:
      - string
      - "null"
    description: Fund group currency
  managing_branch:
    type:
      - string
      - "null"
    description: Branch responsible for managing the resource
  ledgers:
    type: array
    description: List of ledgers
    items:
      $ref: "acq_ledger.yaml"
  funds:
    type: array
    description: List of funds
    items:
      $ref: "acq_fund.yaml"
  owner:
    type: object
    description: Owner of the ledger
    items:
      $ref: "patron.yaml"
additionalProperties: false

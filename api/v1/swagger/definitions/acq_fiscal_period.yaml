---
type: object
properties:
  fiscal_period_id:
    type:
      - integer
      - "null"
    readOnly: true
    description: Internal fiscal period identifier
  description:
    type:
      - string
      - "null"
    description: Description
  code:
    type:
      - string
      - "null"
    description: Fiscal period code
  start_date:
    type: string
    format: date
    description: Start date for the fiscal period
  end_date:
    type:
      - string
      - "null"
    format: date
    description: End date for the fiscal period
  spend_limit:
    type:
      - number
      - "null"
    description: Spend limit of the fiscal period
  last_updated:
    type: string
    format: date-time
    readOnly: true
    description: Last update
  status:
    description: Is the fiscal period active
    type: boolean
  owner_id:
    type:
      - integer
      - "null"
    description: Owner of the fiscal period
  managing_branch:
    type:
      - string
      - "null"
    description: Branch responsible for managing the resource
  ledgers:
    type: array
    description: List of ledgers
    items:
      $ref: "acq_ledger.yaml"
  funds:
    type: array
    description: List of funds
    items:
      $ref: "acq_fund.yaml"
  owner:
    type: object
    description: Owner of the ledger
    items:
      $ref: "patron.yaml"
additionalProperties: false

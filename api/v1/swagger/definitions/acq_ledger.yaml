---
type: object
properties:
  ledger_id:
    type:
      - integer
      - "null"
    readOnly: true
    description: Internal ledger identifier
  fiscal_period_id:
    type:
      - integer
      - "null"
    description: Internal fiscal period identifier
  name:
    type:
      - string
      - "null"
    description: Ledger name
  description:
    type:
      - string
      - "null"
    description: Description
  external_id:
    type:
      - string
      - "null"
    description: Ledger external id
  currency:
    type:
      - string
      - "null"
    description: Ledger currency
  modified_date:
    type: string
    format: date-time
    readOnly: true
    description: Last update
  created_date:
    type: string
    format: date-time
    readOnly: true
    description: Date created
  status:
    description: Is the ledger active
    type: boolean
  locked:
    description: Is the ledger locked
    type: boolean
  owner_id:
    type:
      - integer
      - "null"
    description: Owner of the ledger
  managing_branch:
    type:
      - string
      - "null"
    description: Branch responsible for managing the resource
  ledger_amount:
    type:
      - number
      - "null"
    description: Spend limit of the ledger
  oe_warning_percent:
    type:
      - number
      - "null"
    description: Over encumbrance warning percentage
  oe_warning_amount:
    type:
      - number
      - "null"
    description: Over encumbrance warning amount
  fiscal_period:
    type: object
    description: Fiscal period
    items:
      $ref: "acq_fiscal_period.yaml"
  funds:
    type: array
    description: List of funds
    items:
      $ref: "acq_fund.yaml"
  owner:
    type:
      - object
      - "null"
    description: Owner of the ledger
    items:
      $ref: "patron.yaml"
  managing_library:
    description: Information about the managing library for the ledger
    type:
      - object
      - "null"
  allocations:
    type: array
    description: List of allocations
    items:
      $ref: "acq_allocation.yaml"
  child_object_managing_branches:
    type: array
    description: List of managing branches of any child objects
    items:
      $ref: "acq_child_object_managing_branch.yaml"
additionalProperties: false

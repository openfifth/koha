---
type: object
properties:
  ledger_id:
    type:
      - integer
      - "null"
    readOnly: true
    description: Internal ledger identifier
  fiscal_period_id:
    type:
      - integer
      - "null"
    description: Internal fiscal period identifier
  name:
    type:
      - string
      - "null"
    description: Ledger name
  description:
    type:
      - string
      - "null"
    description: Description
  code:
    type:
      - string
      - "null"
    description: Ledger code
  external_id:
    type:
      - string
      - "null"
    description: Ledger external id
  currency:
    type:
      - string
      - "null"
    description: Ledger currency
  last_updated:
    type: string
    format: date-time
    readOnly: true
    description: Last update
  status:
    description: Is the ledger active
    type: boolean
  owner_id:
    type:
      - integer
      - "null"
    description: Owner of the ledger
  managing_branch:
    type:
      - string
      - "null"
    description: Branch responsible for managing the resource
  spend_limit:
    type:
      - number
      - "null"
    description: Spend limit of the ledger
  ledger_value:
    type:
      - number
      - "null"
    description: Value of the ledger
  over_spend_allowed:
    description: Is over spend allowed on the ledger
    type: boolean
  oe_warning_percent:
    type:
      - number
      - "null"
    description: Over encumbrance warning percentage
  oe_limit_amount:
    type:
      - number
      - "null"
    description: Over encumbrance limit amount
  os_warning_sum:
    type:
      - number
      - "null"
    description: Over spend warning sum
  os_limit_sum:
    type:
      - number
      - "null"
    description: Over spend limit sum
  fiscal_period:
    type: object
    description: Fiscal period
    items:
      $ref: "acq_fiscal_period.yaml"
  funds:
    type: array
    description: List of funds
    items:
      $ref: "acq_fund.yaml"
  owner:
    type: object
    description: Owner of the ledger
    items:
      $ref: "patron.yaml"
  managing_library:
    description: Information about the managing library for the ledger
    type:
      - object
      - "null"
additionalProperties: false

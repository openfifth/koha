---
type: object
properties:
  allocation_id:
    type:
      - integer
      - "null"
    readOnly: true
    description: Internal fund identifier
  fund_id:
    type:
      - integer
      - "null"
    description: Internal fund identifier
  ledger_id:
    type:
      - integer
      - "null"
    description: Internal ledger identifier
  allocation_amount:
    type:
      - number
      - "null"
    description: Amount allocated to the fund
  reference:
    type:
      - string
      - "null"
    description: Allocation reference
  note:
    type:
      - string
      - "null"
    description: Allocation note
  type:
    type:
      - string
      - "null"
    description: Type of the allocation
  is_transferred_to:
    type:
      - integer
      - "null"
    description: Entity receiving the allocation
  is_transferred_from:
    type:
      - integer
      - "null"
    description: Entity sending the allocation
  created_date:
    type: string
    format: date-time
    readOnly: true
    description: Date created
  owner_id:
    type:
      - integer
      - "null"
    description: Owner of the fund
  managing_branch:
    type:
      - string
      - "null"
    description: Branch responsible for managing the resource
  ledger:
    type: object
    description: Ledger
    items:
      $ref: "acq_ledger.yaml"
  fund:
    type: object
    description: Fund
    items:
      $ref: "acq_fund.yaml"
  owner:
    type: object
    description: Owner of the ledger
    items:
      $ref: "patron.yaml"
additionalProperties: false
